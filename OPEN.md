# OPEN — one line per open item, greppable; swept by /gc

- product (owner ruling needed): post-Step-5 the UI has NO click-path to a policy detail page — catalog shows Services only, search excludes policies, only a policy page links policies (circular); route works by direct URL. Intended consequence of "policies fold onto services" or a gap? Dead code sits in `components/catalog/adopted.tsx` (`tab === "policies"` branch, PolicyCard, policy TableView). Found 2026-07-06 during e2e spec repair.
- graph: `SNAPSHOT_CAS_LUA` is mirrored by a fake in CI, never executed against a live Valkey — a Lua-side drift would not fail CI (accepted residual risk, Step-2 tails FIX 6; candidate for a live-Valkey integration test if infra allows).
- graph: snapshot-store degraded-fallback recovery (60s retry) is tested with injected clock/deps only; no chaos test against a real Valkey outage.
- lint: two pre-existing oxlint warnings in `briefs/assembleBrief.ts` + `sourceContent/confluenceOnboardingProvider.ts` (untouched, Step-4 vintage).
- step-2 deferred (by design, parent goal prompt): subscriptions + push delivery (Atom is the pull channel); A2 cadence measurement once the differ can replay source version history.
- step-6/7: deferred Codex review pass RUN + ALL findings resolved on `feat/1.0.0` (2026-07-07). Step 7 (4): DELETE now APP-scoped (404 on cross-APP), TFE fetch targets the url-named workspace (org/name parsed + hardened, SSRF still base-pinned), debug floor carries at-read values (new optional `BriefBlock.statuses[]`, D8/locked-7 parity with `/api/status`), no-base⇒no-tokened-fetch. Step 6 (2): change-path pino log (D2) added; Entra-doc scope drift kept as owner-approved exception. Verdicts absorbed into `implementation-notes-step7.md` + `step6-instruments-implementation-notes.md`. Whole repo green (context-layer 396/2skip, schema 82, portal 168, acceptance 10, infra 8). Both codex-prompt files now /gc archive candidates. (Follow-up: portal debug-brief view does not yet render `statuses[]` — API/MCP parity done, portal UI render TBD.)
- merge: Step 6 (`feat/step6-instruments`) + Step 7 (`feat/step7-status-board`) both integrated into `feat/1.0.0` (merge 6696919, 2026-07-07); full typecheck + `pnpm -r test` green (context-layer 392/2skip, portal 168, acceptance 10, schema 80, infra 8). e2e primary not re-run on the integrated tree (green on each parent; merge touched no route wiring). (`goal_prompt_entra_app_scope.md` anchors re-verified + ratified at `0880d7a`, 2026-07-07.)
- entra: slice LANDED on `feat/1.0.0` (2026-07-07) — Opus build + Fable review (5 findings fixed: form_post/Lax tx cookie, mock-directory prod leak, R20 Seam-B, E3/E4 test gaps, cert loud-fail) + Codex review (4 findings fixed: prod in-memory session loud-fail, MSAL cache stripped to refresh material, HTTP-composition honest-gap WARN, principal→mutation attribution log). Green: typecheck all, context-layer 446/2skip · portal 200 · schema 89 · acceptance 10 · infra 8, e2e primary 42. Deviation log: `docs/architecture/entra-implementation-notes.md`.
- entra follow-on (out of slice, ratified Q1/R16): WS3 MCP OAuth (401+`WWW-Authenticate`, gated on the A1 no-DCR public-client/broker ruling); silent refresh not implemented — session dies at id_token expiry (~1h, re-login; refresh material IS stored); HTTP Context-API composition drops browser identity (fail-closed + once-per-process WARN) until the OBO/two-registration follow-on (open question 4).
- entra nits (accepted): `valkeySessionStore` prod-only untested (like `valkeyContentCache`); logout is GET-triggerable (logout CSRF, low impact); callback failure branch returns 401 with an unused `location` header.

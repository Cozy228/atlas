{
  "name": "@atlas/infra",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "fmt:check": "terraform fmt -recursive -check",
    "validate": "terraform init -backend=false && terraform validate",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "4.1.9"
  }
}
